// use std::io::{stdout};
// use ferris_says::say;

fn main() {
    println!("hi");
    // let writer = BufWriter::new(stdout().lock());
    // let cosa: Result<(), std::io::Error> = say("input", 5,stdout());
}
